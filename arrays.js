const names = ["Alexander", "Kate", "Ollie", "Shauna"];
console.log(names[0]); // Alexander

const numberOfPets = [1, 0, 2, 3];
console.log(numberOfPets[2]); // 2

// We can get the length of an array using the length properly
console.log(names.length); //4